# Server_Tic-Tac-Toe

This project represents the **Server Application** for the Tic-Tac-Toe multiplayer game.  
The server manages client connections, game sessions, and communication between players.

---

## 🖥️ Server Interface

### Main Server Window
![Server UI](https://github.com/user-attachments/assets/2aba3e61-acf6-4bc9-acbd-de709e40971b)

### Server Icon
![Server Icon](https://github.com/user-attachments/assets/f5dafcf7-0b4b-4d58-971c-4040c6a0de19)

---

## 📦 Requirements

- Windows Operating System
- No Java or JDK installation required when using the `.exe` file

---

## ▶️ How to Run the Server

### Method 1: Using the Executable File (.exe)
2. Double-click the `.exe` file.
3. The server window will open.
4. Click **Start Server** (if available).
5. The server status will change to **Running** and start listening for clients.

✅ The server is now ready to accept connections.

---

### Method 2: Run from Source Code (For Developers)

1. Install **Java JDK**.
2. Open the project using **NetBeans / IntelliJ IDEA / Eclipse**.
3. Run the main server class.
4. The server UI will appear and start listening on the configured port.

---

## ⛔ How to Stop the Server

### Option 1: From the Server UI
- Click **Stop Server** to safely close all connections.

### Option 2: Close the Window
- Click the **X** button on the server window.
- The server will shut down automatically.

⚠️ It is recommended to disconnect all clients before stopping the server.

---

## 🌐 Network Information

- The server runs on **localhost** or the machine's **local IP address**.
- Clients must use the same **IP address** and **port number** to connect.
- Make sure the firewall allows the server application if clients are on different machines.

---

## 📁 Project Structure

Server_Tic-Tac-Toe
│── Server_Tic-Tac-Toe.exe
│── src/
│── resources/


1. Download the server executable file:
